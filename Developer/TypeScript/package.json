{
  "name": "lyra-puerts-entry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.plugin.json && tsc -p tsconfig.plugin-editor.json && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.plugin.json --noEmit && tsc -p tsconfig.plugin-editor.json --noEmit && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
